# ===========================================
# The Coffee Links Server - Environment Variables
# ===========================================
# Copy this file to .env.local and fill in your values

# Supabase Configuration (REQUIRED)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Encryption Key (REQUIRED for admin login)
# Generate with: openssl rand -base64 32
ENCRYPTION_KEY=your-32-character-encryption-key

# Admin Authentication (REQUIRED for admin panel)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your-secure-admin-password
ADMIN_SECRET=your-admin-session-secret

# Staff API Authentication (REQUIRED for staff app)
# Generate with: openssl rand -hex 32
STAFF_API_SECRET=your-staff-api-secret-key

# CORS Configuration (Optional - defaults to common origins)
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=https://your-admin.vercel.app,https://your-staff.vercel.app,http://localhost:3000

# Payment Configuration
# Options: 'production', 'sandbox', 'bypass'
# - production: Requires real payment gateway (fails without)
# - sandbox: Validates inputs but returns mock success (default)
# - bypass: No validation (DEVELOPMENT ONLY)
PAYMENT_MODE=sandbox

# Rate Limiting (Future implementation)
# RATE_LIMIT_LOGIN=10/minute
# RATE_LIMIT_API=100/minute

# Logging Level (Optional)
LOG_LEVEL=info
